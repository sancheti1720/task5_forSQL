# Task 5 – SQL Joins (Inner, Left, Right, Full)

This task explains how to combine data from multiple relational
tables using SQL Joins.

## Tables Used
1. customers(customer_id, name, city)
2. orders(order_id, customer_id, product, amount)

## Concepts Covered
- INNER JOIN
- LEFT JOIN
- RIGHT JOIN
- FULL OUTER JOIN (using UNION)
- CROSS JOIN
- SELF JOIN
- Joining more than two tables

## SQL Queries Included
- Customers with their orders (INNER JOIN)
- All customers even without orders (LEFT JOIN)
- All orders even without customer details (RIGHT JOIN)
- Full data merge using UNION
- Cross join (Cartesian product)
- Self join (customers from same city)
- 3-table join example
